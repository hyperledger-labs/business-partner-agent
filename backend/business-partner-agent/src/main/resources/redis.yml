micronaut:
  session:
    http:
      redis:
        enabled: true
        namespace: 'bpa:sessions'
        write-mode: BACKGROUND
        enable-keyspace-events: true
        valueSerializer: io.micronaut.jackson.serialize.JacksonObjectSerializer

redis:
  uri: redis://localhost